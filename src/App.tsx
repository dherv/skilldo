import React from "react";
import TodoSettings from "./components/TodoSettings";

function App() {
  return (
    <div>
      <TodoSettings onTogglePopover={() => {}} />
    </div>
  );
}

export default App;
